@model IEnumerable<WebCustumerManagerApp.Models.EntityOccupationGroup>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OccupationGroupTitle)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OccupationGroupTitle)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.OccupationGroupId }) |
                @Html.ActionLink("Details", "Details", new { id=item.OccupationGroupId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.OccupationGroupId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
